<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Message</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body>
    <div class="container-fluid">
        <div class="chat-container">
            <div class="sidebar">
                <div class="d-flex align-items-center mb-4">
                    <i class="bi bi-whatsapp" style="font-size:2rem;color:#25d366;"></i>
                    <h3 class="ms-2 mb-0 fw-bold">Whatsaap</h3>
                </div>
                <a href="/chats" class="btn btn-outline-primary w-100 mb-3"><i class="bi bi-arrow-left me-2"></i>Back to
                    Chats</a>
                <hr>
                <div>
                    <strong class="d-block mb-2"><i class="bi bi-pencil-square me-2"></i>Edit message</strong>
                </div>
            </div>
            <div class="form-area">
                <div class="form-box shadow-lg">
                    <h4 class="mb-4 text-center"><i class="bi bi-pencil-square me-2"></i>Edit Your Message</h4>
                    <p class="mb-3 text-center">This chat was sent from <span class="badge bg-primary"><i
                                class="bi bi-person-circle me-1"></i>
                            <%= info.from %>
                        </span> to <span class="badge bg-success"><i class="bi bi-person me-1"></i>
                            <%= info.to %>
                        </span></p>
                    <form action="/chats/<%= info.id %>/edit?_method=PUT" method="post">
                        <div class="mb-3 input-group">
                            <span class="input-group-text bg-light"><i class="bi bi-chat-text"></i></span>
                            <textarea name="msg" rows="5" class="form-control" placeholder="Edit your message"
                                required><%= info.msg %></textarea>
                        </div>
                        <button class="btn btn-warning w-100 py-2"><i class="bi bi-save me-1"></i>Update</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>